<h1>写真再アップロード／タイトル編集</h1>

<%= render 'form', picture: @picture %>
<%= render 'details', picture: @picture %> |
<%= link_to "削除", picture_path(@picture.id), method: :delete %>
<%= link_to 'キャンセル', pictures_path %>
